const express=require("express");const protect=require("../middleware/authMiddleware");const c=require("../controllers/featureController");const r=express.Router();
r.post("/recipes/:id/view",c.viewRecipe);r.get("/recipes/:id/reviews",c.getReviews);r.post("/recipes/:id/reviews",protect,c.upsertReview);r.get("/notifications",protect,c.getNotifications);r.put("/notifications/read",protect,c.markNotificationsRead);r.post("/orders",protect,c.createOrder);r.get("/orders",protect,c.getOrders);r.get("/recommendations",protect,c.getRecommendations);r.get("/admin/stats",protect,c.adminStats);module.exports=r;
